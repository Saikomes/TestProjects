const testNewsTitle = 'Тестовая новость'

const testPreview = 'Тестовое превью'

const testContent = 'Тестовое содержимое'

const testNewsGroups = ["Все"]

const NewsConfig = {
    testNewsTitle,
    testPreview,
    testContent,
    testNewsGroups
}

export default NewsConfig;

